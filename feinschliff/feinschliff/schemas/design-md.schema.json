{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://feinschliff.dev/schemas/design-md.schema.json",
  "title": "DESIGN.md frontmatter (Feinschliff brand contract)",
  "description": "Subset of Google's DESIGN.md spec (Apache 2.0, github.com/google-labs-code/design.md) used by Feinschliff brands. The runtime ships static .pptx; this is build-time only.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "colors"],
  "properties": {
    "version": {
      "type": "string",
      "description": "Spec version of upstream DESIGN.md. Currently 'alpha'."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Display name of the brand."
    },
    "description": {
      "type": "string",
      "description": "One-line summary."
    },
    "colors": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "string",
        "pattern": "^#[0-9a-fA-F]{6}$",
        "description": "Hex color, exactly 7 chars (#RRGGBB), no alpha."
      }
    },
    "typography": {
      "type": "object",
      "description": "Typography tokens. v1 supports only 'inherit: <base-brand>'."
    },
    "spacing": {
      "type": "object",
      "description": "Spacing scale. Reserved for future use."
    },
    "rounded": {
      "type": "object",
      "description": "Corner-radius scale. Reserved for future use."
    },
    "components": {
      "type": "object",
      "description": "Component-level tokens. Reserved for future use."
    }
  }
}
