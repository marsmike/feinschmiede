name: pages

on:
  # Disabled during the master-template migration (PR 0). The gallery
  # script lives in feinschliff-builder/ which is being deleted; PR 8
  # rebuilds the gallery from soffice + pdftoppm against the new
  # master-template renderer and re-enables the push trigger.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Install workspace (all packages)
        # The gallery script lives in feinschliff-builder/scripts/ post-split
        # and reads brand packs from both feinschliff/ and feinschliff-extra/,
        # so install the whole workspace.
        run: uv sync --all-packages
      - name: Build brand-gallery site
        working-directory: feinschliff-builder
        run: uv run python scripts/build_brand_gallery_site.py
      - name: Add root redirect to /brands/
        run: |
          cat > docs/index.html <<'HTML'
          <!doctype html>
          <meta charset="utf-8">
          <title>feinschmiede brand gallery</title>
          <link rel="icon" type="image/svg+xml" href="feinschmiede-mark.svg">
          <meta property="og:title" content="feinschmiede — brand gallery">
          <meta property="og:image" content="https://marsmike.github.io/feinschmiede/feinschmiede-social.png">
          <meta name="twitter:card" content="summary_large_image">
          <meta http-equiv="refresh" content="0; url=brands/">
          <link rel="canonical" href="brands/">
          <p>Redirecting to the <a href="brands/">feinschmiede brand gallery</a>…</p>
          HTML
      - name: Add brand assets to the Pages site
        run: cp assets/feinschmiede-mark.svg assets/feinschmiede-mark-dark.svg assets/feinschmiede-social.png docs/
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
